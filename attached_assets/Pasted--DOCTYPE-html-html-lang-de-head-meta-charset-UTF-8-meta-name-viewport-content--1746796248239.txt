<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Fehlerkatalog</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    textarea { width: 100%; height: 150px; margin-bottom: 10px; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { border: 1px solid #ccc; padding: 8px; text-align: center; }
    th { background-color: #f0f0f0; }
    .generate-btn { padding: 10px 20px; font-size: 16px; }
  </style>
</head>
<body>
  <h1>Fehlerkatalog Bulkimport</h1>

  <p>Füge unten eine Liste von Fehlern ein (einer pro Zeile):</p>
  <textarea id="bulkInput" placeholder="z. B.&#10;Displaybruch&#10;Akku defekt&#10;Wasserschaden"></textarea>
  <br>
  <button class="generate-btn" onclick="generateTable()">Tabelle erzeugen</button>

  <table id="errorTable">
    <thead>
      <tr>
        <th>Fehler</th>
        <th>Smartphone</th>
        <th>Tablet</th>
        <th>Laptop</th>
        <th>Smartwatch</th>
      </tr>
    </thead>
    <tbody></tbody>
  </table>

  <script>
    function generateTable() {
      const input = document.getElementById('bulkInput').value.trim();
      const lines = input.split('\n').map(line => line.trim()).filter(line => line);
      const sortedLines = [...new Set(lines)].sort((a, b) => a.localeCompare(b, 'de'));
      const tbody = document.querySelector('#errorTable tbody');
      tbody.innerHTML = ''; // reset table

      for (const error of sortedLines) {
        const row = document.createElement('tr');
        const errorCell = document.createElement('td');
        errorCell.textContent = error;
        row.appendChild(errorCell);

        for (let i = 0; i < 4; i++) {
          const cell = document.createElement('td');
          const checkbox = document.createElement('input');
          checkbox.type = 'checkbox';
          cell.appendChild(checkbox);
          row.appendChild(cell);
        }

        tbody.appendChild(row);
      }
    }
  </script>
</body>
</html>
