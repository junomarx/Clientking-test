<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Abholschein 58mm</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      font-size: 11px;
      width: 58mm;
      margin: 0;
      padding: 10px;
      color: #000;
    }

    .logo {
      text-align: center;
      margin-bottom: 5px;
    }

    .logo-img {
      max-width: 90%;
      height: auto;
    }

    .company,
    .top-info {
      text-align: center;
    }

    .company {
      margin-bottom: 10px;
    }

    .top-info {
      margin: 10px 0 15px;
    }

    .headline {
      font-weight: bold;
      font-size: 14px;
      margin-bottom: 2px;
    }

    .auftragsnummer {
      font-weight: bold;
      font-size: 12px;
    }

    .section {
      margin-bottom: 14px;
    }

    .field {
      margin-bottom: 3px;
    }

    .kundenname,
    .geraetinfo {
      font-size: 12px;
      font-weight: bold;
      margin-bottom: 2px;
    }

    .schaden-title {
      font-weight: bold;
      margin-top: 6px;
      margin-bottom: 2px;
    }

    .preis-label {
      font-weight: bold;
      font-size: 11px;
      margin-top: 8px;
      margin-bottom: 2px;
    }

    .signature-box {
      margin-top: 16px;
      text-align: center;
    }

    .signature-title {
      font-weight: bold;
      margin-bottom: 4px;
    }

    .signature-line {
      margin-top: 20px;
      border-top: 1px solid #000;
      width: 100%;
    }

    .terms-box {
      border: 1px solid #000;
      padding: 6px;
      font-size: 9px;
      line-height: 1.3;
    }

    .terms-title {
      text-align: center;
      font-weight: bold;
      font-size: 10px;
      margin-bottom: 4px;
    }

    @media print {
      @page {
        size: 58mm auto;
        margin: 0;
      }
      body {
        margin: 0;
        padding: 5px;
      }
    }
  </style>
</head>
<body>

  <div class="logo">
    <img src="{{ firmenlogo }}" alt="Logo" class="logo-img">
  </div>

  <div class="company">
    <strong>Mac and PhoneDoc</strong><br>
    Amerlingstraße 19<br>
    1060 Wien<br>
    +43 1 410 35 11
  </div>

  <div class="top-info">
    <div class="headline">Abholschein</div>
    <div class="auftragsnummer">{{ auftragsnummer }}</div>
    <div>{{ datum_dropoff }}</div>
  </div>

  <div class="section">
    <div class="kundenname">{{ kundenname }}</div>
    <div class="field">{{ kundentelefon }}</div>
    <div class="field">{{ kundenemail }}</div>
  </div>

  <div class="section">
    <div class="geraetinfo">{{ hersteller }} {{ modell }}</div>

    <div class="schaden-title">Schaden/Fehler</div>
    <div class="field">{{ problem }}</div>

    <div class="preis-label">Preis</div>
    <div class="field">{{ preis }}</div>
  </div>

  <div class="section">
    <div class="terms-box">
      <div class="terms-title">Reparaturbedingungen</div>
      1. Keine Haftung für Datenverlust – Kunde ist verantwortlich.<br><br>
      2. Reparatur mit geprüften, ggf. nicht originalen Teilen.<br><br>
      3. 6 Monate Gewährleistung auf Reparaturleistung.<br><br>
      4. Zugriff auf Gerät zur Fehlerprüfung möglich.<br><br>
      5. Abholung innerhalb von 60 Tagen erforderlich.<br><br>
      6. Mit Unterschrift werden Bedingungen akzeptiert.
    </div>
  </div>

  <div class="signature-box">
    <div class="signature-title">Reparaturauftrag erteilt</div>
    <div class="signature-line">{{ signatur_dropoff }}</div>
    {{ kundenname }}<br>
    {{ datum_dropoff }}
  </div>

  <div class="signature-box">
    <div class="signature-title">Gerät abgeholt</div>
    <div class="signature-line">{{ signatur_pickup }}</div>
    {{ kundenname }}<br>
    {{ datum_pickup }}
  </div>

</body>
</html>
